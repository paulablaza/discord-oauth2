name: Create Docs
on: [push]

jobs:
  Create-Docs:
    runs-on: ubuntu-latest

    steps:
      - run: echo ${{ github.workspace }}
      - run: pip install portray
      - run: python -m portray as_html -o portraydocs -m ${{ github.workspace }}/discord_outh2 discord_oauth2 --overwrite
